// src/styles/theme.js
import { colorTokens, spacingTokens, fontFamily, fontSize, fontWeight, lineHeight } from './tokens';

export const theme = {
  colors: colorTokens,
  spacing: spacingTokens,
  typography: {
    fontFamily,
    fontSize,
    fontWeight,
    lineHeight,
  },
};
